| $('#dataset-overlay').show();
| $('#dataset-overlay .content').html("#{ j render("datasets/dataset", dataset: @dataset, commit: @commit) }");
| $('.content-map .container #search-bar').hide();
| DatasetInterface.enable();
