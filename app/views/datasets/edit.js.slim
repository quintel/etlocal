| $('#dataset-overlay .content').html("#{ j render("commits/form", dataset: @dataset, commit: @commit, in_version: @in_version) }");
| $('#dataset-overlay, .content.tab#input').show();
| $('.content-map .container #search-bar-holder').hide();
| DatasetInterface.enable("#{ @dataset.id }");
